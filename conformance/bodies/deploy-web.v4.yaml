# The server APPENDED a container (a sidecar was injected). The array's length changed.
#
# Without an OpenAPI associative-list schema, arrays are intentionally atomic. A positional merge
# could apply a local edit to the wrong element after this server-side append. RFC 7386 also replaces
# arrays as a whole.
apiVersion: apps/v1
kind: Deployment
metadata:
  uid: deploy-web-0001
  name: web
  namespace: app
  resourceVersion: "3004"
  generation: 5
  labels:
    app.kubernetes.io/name: web
spec:
  replicas: 2
  paused: false
  selector:
    matchLabels:
      app: web
  template:
    metadata:
      labels:
        app: web
    spec:
      containers:
        - name: web
          image: ghcr.io/configbutler/web:v1.2.3
          ports:
            - containerPort: 8080
        - name: otel-sidecar
          image: ghcr.io/configbutler/otel:v0.9.0
status:
  observedGeneration: 5
  replicas: 2
  readyReplicas: 2
  conditions:
    - type: Available
      status: "True"
      reason: MinimumReplicasAvailable

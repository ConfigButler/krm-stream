# krm-stream

Compatibility package for the scoped client:

```bash
npm install @configbutler/krm-stream
```

New code should import from `@configbutler/krm-stream`. This unscoped package re-exports the scoped
package so older snippets that import `krm-stream` keep working.
